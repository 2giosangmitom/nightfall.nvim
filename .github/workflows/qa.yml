name: Quality Assurance
on:
  push:
    branches: [master]
  pull_request:

jobs:
  style:
    name: Stylua Formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: JohnnyMorganz/stylua-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: latest
          args: --check .

  spellcheck:
    name: Spellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: crate-ci/typos@v1.21.0

  test:
    name: Test
    strategy:
      matrix:
        version: [v0.10.0, v0.9.5]
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Neovim for Linux
        if: ${{ matrix.os == 'ubuntu-latest' }}
        env:
          VERSION: ${{ matrix.version }}
        run: |
          wget -q https://github.com/neovim/neovim/releases/download/$VERSION/nvim-linux64.tar.gz
          tar -xf nvim-linux64.tar.gz
          ln -sf nvim-linux64/bin/nvim /usr/local/bin

      - name: Run tests
        run: make test
